# Next.js dev proxy target (next.config.js rewrites /api → backend)
BACKEND_URL=http://127.0.0.1:8000

# Only set for production when frontend talks to API directly (no proxy):
# NEXT_PUBLIC_API_URL=https://your-api.example.com
