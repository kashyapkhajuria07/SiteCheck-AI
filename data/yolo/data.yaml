# Roboflow: construction-element-detection (beam, column)
# Paths resolved relative to repo root when training from project root.

path: data/yolo
train: train/images
val: valid/images
test: test/images

nc: 2
names:
  0: beam
  1: column
